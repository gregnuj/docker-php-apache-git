#!/bin/sh
set -e

## install project
if [ -n "$PROJECT_VCS" ]; then
    git clone $PROJECT_VCS $PROJECT_NAME
fi

# first arg is `-f` or `--some-option`
if [ "${1#-}" != "$1" ]; then
        set -- php "$@"
fi

exec "$@"
